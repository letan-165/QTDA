server:
  port: 8080
  servlet:
    context-path: /api


spring:
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://root:root@localhost:3306/QTDA}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

key:
  value: ${KEY_JWT:XDHTTTQLHTSVTPD2025@TPD#SystemKey!}

